en api - index.js:

inicializamos nuestro server. Con force false hacemos que cada vez que actualizamos información, la información dentro de nuestra DB no se borre.

en db.js: 

armar la relación entre mis dos modelos (belongsToMany), pokemons y types, y definir el model a traves del cual se conectan. 

defini una carpeta para los controllers. Defino la lógica de las rutas dentro de ellos.

en routes - index.js:

modularizo las rutas.

